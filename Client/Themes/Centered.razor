@namespace [Owner].Cre8magic.Theme.[Theme]
@inherits ThemeBase

@code {
    public override string Name => "Centered";
    public override string Panes => PaneNames.Default + ",Header";
}

<Default Variant="centered"></Default>
@namespace [Owner].Theme.[Theme]
@inherits ThemeBase

@code {
    public override string Name => "Fullscreen";
    public override string Panes => PaneNames.Default + ",Header";
}

<Default Variant="fullscreen"></Default>